# Анализ производительности пайплайна кластеризации

**Дата:** 2026-06-20  
**Датасет:** D:\FileOrganizer\Sorted\PDF_Text (22,180 файлов)  
**Прогнозное время обработки:** ~5 часов  

---

## Результаты бенчмарков извлечения текста (первая страница, 10 PDF с таблицами)

### PyMuPDF vs PyMuPDF4LLM

| Метод | Warmup | Total (10 файлов) | Среднее/файл | Относительно PyMuPDF |
|---|---|---|---|---|
| `page.get_text()` | 6ms | **37ms** | ~4ms | 1x (базовая) |
| `pymupdf4llm.to_markdown(pages=[0])` | 552ms | **5,522ms** | ~552ms | **~149x медленнее** |

### PyMuPDF native tables (`find_tables().to_markdown()`)

| Метод | Время | Текст |
|---|---|---|
| `get_text()` | 6ms | 3,581 chars |
| Нативные таблицы | 1,418ms | 7,239 chars (мусорный) |
| PyMuPDF4LLM | 824ms | 4,037 chars (структурированный) |

### Aspose.PDF vs PyMuPDF

| Метрика | Aspose.PDF (.NET) | PyMuPDF/fitz |
|---|---|---|
| Warmup | 301ms | **5ms** |
| Total (10 файлов) | **957ms** | **37ms** |
| Среднее на файл | ~96ms | **~4ms** |

> Aspose.PDF не смог обработать 89 из 100 случайных PDF (`IncorrectFontUsageException`). PyMuPDF обработал все файлы.

### Статистика по таблицам (выборка 500 файлов)

| Категория | Кол-во | Доля |
|---|---|---|
| С таблицами (`page.find_tables()`) | 402 | **80.4%** |
| Без таблиц | 98 | 19.6% |

---

## Оценка времени по этапам (для 22K PDF)

| Этап | Что делает | Время/файл | Общее время | Доля |
|---|---|---|---|---|
| **Stage 2: Извлечение** | PyMuPDF4LLM на каждом PDF | ~600ms | **~3.6 часа** | **75%** ← ГЛАВНЫЙ БОТЛЕНЕК |
| Stage 5: Кластеризация | Матрица расстояний N×N + AgglomerativeClustering | O(N²) | ~12 мин | 8% |
| Stage 3: LLM саммаризация | Qwen3.5-4B, batch=4 | ~2s среднее | ~12 мин | 8% |
| Stage 4: Эмбеддинги | nomic-embed-text-v1.5, batch=32 | ~0.1s | ~4 мин | 3% |
| Stage 1 + прочее | Сортировка, перемещение файлов | ~5ms | ~10 мин | 6% |

---

## Конкретные проблемы и решения

### Проблема 1: PyMuPDF4LLM на КАЖДОМ PDF (критическая)

**Файл:** `extractors/pdf_extractor.py`, строка 69  
**Код:**
```python
text_p4llm = _extract_text_pymupdf4llm(file_path)  # ~500-800ms на файл!
```

Каждый PDF проходит через ONNX-модель анализа макета, даже если текст простой и линейный.

**Решение:** Гибридное извлечение с фильтрацией по наличию таблиц:

```python
# Быстрая проверка (~2ms на файл)
doc = fitz.open(str(file_path))
page = doc[0]
tables = page.find_tables()
has_tables = tables and len(tables.tables) > 0

if has_tables:
    # Сложный документ с таблицами → PyMuPDF4LLM (~550ms)
    text_p4llm = _extract_text_pymupdf4llm(file_path)
else:
    # Простой текст → обычный PyMuPDF (~2ms)
    text_raw = page.get_text("text")
```

**Экономия:** Для 20% файлов без таблиц — ~1.5 часа. Для файлов с таблицами можно рассмотреть нативное извлечение `tab.to_markdown()` как fallback.

---

### Проблема 2: Кластеризация O(N²) (средняя)

**Файл:** `cluster_engine.py`, строка 38  
**Код:**
```python
distance_matrix = np.empty((n, n), dtype=np.float32)  # Для 22K = 1.9 GB!
```

Полная матрица расстояний для 22K документов занимает ~1.9 ГБ и требует O(N²) вычислений. AgglomerativeClustering на CPU — медленно.

**Решение:** Заменить на HDBSCAN:

```python
from hdbscan import HDBSCAN

clustering = HDBSCAN(
    metric='cosine',
    min_cluster_size=cfg.MIN_CLUSTER_SIZE,
    cluster_selection_epsilon=1.0 - cfg.SIMILARITY_THRESHOLD,
)
labels = clustering.fit_predict(embeddings)  # Без матрицы расстояний!
```

Преимущества HDBSCAN:
- Не требует полной матрицы расстояний (работает напрямую с эмбеддингами)
- В 5-10 раз быстрее для больших датасетов
- Автоматически определяет шум (неклассифицированные документы)
- Поддерживает иерархическую кластеризацию

**Экономия:** ~12 мин → 1-2 мин

---

### Проблема 3: LLM саммаризация с малым batch size (низкая)

**Файл:** `config.py`, строка 70  
**Текущее значение:** `HF_BATCH_SIZE = 4`

Qwen3.5-4B на RTX PRO 4000 Blackwell (24 GB VRAM) может обрабатывать большие батчи.

**Решение:** Увеличить `HF_BATCH_SIZE` до 8-16:
```python
HF_BATCH_SIZE = 16  # Вместо 4
```

**Экономия:** ~12 мин → 5-7 мин (при условии достаточного VRAM)

---

### Проблема 4: LLM саммаризация не обязательна для кластеризации

Если конечная цель — кластеризация по содержанию, эмбеддинги от `nomic-embed-text-v1.5` уже содержат семантическую информацию. LLM-саммаризация добавляет метки ТЕМА/ТИП, но без неё кластеризация всё равно работает.

**Решение:** Отключить для быстрых итераций:
```python
LLM_ENABLED = False  # config.py
```

**Экономия:** ~12 мин

---

## Рекомендации по приоритету внедрения

| Приоритет | Изменение | Файл(ы) | Экономия времени | Сложность |
|---|---|---|---|---|
| **P0** | Гибридное извлечение: `find_tables()` → PyMuPDF4LLM только для PDF с таблицами | `extractors/pdf_extractor.py` | ~1-3 часа | Низкая |
| **P1** | HDBSCAN вместо AgglomerativeClustering | `cluster_engine.py`, `requirements.txt` | ~10 мин → 1-2 мин | Средняя (нужен `pip install hdbscan`) |
| **P2** | Увеличить `HF_BATCH_SIZE` до 8-16 | `config.py` | ~12 мин → 5-7 мин | Низкая |
| **P3** | Опционально: отключить LLM саммаризацию для быстрых итераций | `config.py` (`LLM_ENABLED=False`) | ~12 мин | Низкая |

---

## Итоговая визуализация распределения времени

```
PyMuPDF4LLM на каждом PDF:    ████████████████████████████  ~3.6 часа (75%)
Кластеризация O(N²):          ████░░░░░░░░░░░░░░░░░░░░░░░░   ~12 мин  (8%)  
LLM саммаризация:             ████░░░░░░░░░░░░░░░░░░░░░░░░   ~12 мин  (8%)
Эмбеддинги:                   ██░░░░░░░░░░░░░░░░░░░░░░░░░░░░    ~4 мин  (3%)
Сортировка + прочее:          ████░░░░░░░░░░░░░░░░░░░░░░░░   ~10 мин  (6%)
```

**Ожидаемое время после внедрения P0+P1+P2:** ~45-60 минут (вместо 5 часов)
